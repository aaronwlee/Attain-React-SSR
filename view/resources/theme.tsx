import { MatUI } from "../deps.tsx";
const theme = MatUI.createMuiTheme({
  palette: {
    primary: {
      main: "#556cd6",
    },
    secondary: {
      main: "#19857b",
    },
    error: {
      main: "#19857b",
    },
    background: {
      default: "#fff",
    },
  },
});

export default theme;
